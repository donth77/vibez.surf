name = "suno-share-resolver"
main = "src/worker.ts"
compatibility_date = "2024-06-01"

# Optional — restrict CORS to your app origin instead of "*".
# [vars]
# ALLOWED_ORIGIN = "https://your-app.example.com"
